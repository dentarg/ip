{
  "name": "ip",
  "version": "1.0.0",
  "description": "A Cloudflare Worker displaying your IP address and reverse hostname",
  "main": "worker.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write '**/*.js'"
  },
  "license": "MIT",
  "dependencies": {
    "ip-ptr": "^3.0.0"
  },
  "devDependencies": {
    "prettier": "^1.18.2"
  }
}
